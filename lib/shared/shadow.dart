import 'package:flutter/material.dart';

import 'shared.dart';

class Shadows {
  static List<BoxShadow> primaryShadow = [
    BoxShadow(
      color: AppColors.blackD8Color,
      offset: Offset(0, 6),
      blurRadius: 6,
      spreadRadius: -2,
    )
  ];
}
